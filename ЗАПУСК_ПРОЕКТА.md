# 🚀 Инструкции по запуску SubTracker

## 📋 Предварительные требования

1. **Java 17+** - для сборки и запуска проекта
2. **Python 3** - для веб-сервера (для разработки)
3. **Git** - для клонирования репозитория

## 🔧 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <url_репозитория>
cd SubTracker
```

### 2. Запуск Backend (API сервер)

```bash
# Сборка и запуск backend
./gradlew :backend:run
```

Backend будет доступен по адресу: **http://localhost:8080**

**Что происходит при первом запуске:**
- ✅ Создается SQLite база данных `subtracker.db`
- ✅ Создаются необходимые таблицы (users, subscriptions)
- ✅ Добавляется тестовый пользователь: **user/user**
- ✅ Запускается API сервер с CORS поддержкой

### 3. Запуск Web Frontend

**Откройте новый терминал** и выполните:

```bash
# Переход в директорию с HTML файлами
cd web-frontend/src/jsMain/resources

# Запуск простого HTTP сервера
python -m http.server 3000
```

Web-интерфейс будет доступен по адресу: **http://localhost:3000**

## 🌐 Веб-интерфейс

### Страницы приложения:

- **http://localhost:3000/** - страница входа (корневая)
- **http://localhost:3000/register.html** - регистрация новых пользователей
- **http://localhost:3000/dashboard.html** - главная страница после входа

### 👤 Тестовые данные:

**Готовый тестовый аккаунт:**
- Логин: `user`
- Пароль: `user`

## 🔌 API Endpoints

Backend предоставляет следующие endpoints:

### Публичные (без авторизации):
- `POST /register` - регистрация нового пользователя
- `POST /login` - вход в систему (возвращает JWT токен)

### Защищенные (требуют JWT токен):
- `GET /subscriptions` - получить все подписки
- `GET /subscriptions/{userId}` - получить подписки пользователя
- `POST /subscriptions` - создать новую подписку
- `DELETE /subscriptions/{id}` - удалить подписку

### Пример использования API:

```bash
# Вход в систему
curl -X POST http://localhost:8080/login \
  -H "Content-Type: application/json" \
  -d '{"username":"user","password":"user"}'

# Получение подписок (с токеном)
curl -X GET http://localhost:8080/subscriptions \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 🤖 Telegram Bot (опционально)

### Настройка:

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Создайте файл `.env` в корне проекта:
   ```bash
   TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
   ```

### Запуск:

```bash
./gradlew :telegram-bot:run
```

## 📁 Структура проекта

```
SubTracker/
├── backend/                    # Backend API (Ktor + SQLite)
│   ├── src/jvmMain/           # Исходный код
│   │   ├── Application.kt     # Главный файл с API роутами
│   │   ├── DatabaseFactory.kt # Настройка БД и создание тестового пользователя
│   │   ├── UserRepository.kt  # Работа с пользователями
│   │   └── SubscriptionRepositoryImpl.kt # Работа с подписками
│   └── build.gradle.kts       # Зависимости backend
├── web-frontend/              # Web интерфейс
│   └── src/jsMain/resources/  # HTML страницы
│       ├── index.html         # Страница входа (корневая)
│       ├── register.html      # Страница регистрации
│       └── dashboard.html     # Главная страница
├── telegram-bot/              # Telegram бот
├── shared/                    # Общие модели данных
└── subtracker.db             # SQLite база данных (создается автоматически)
```

## 🛠️ Технологии

### Backend:
- **Kotlin/JVM** - основной язык
- **Ktor** - веб-фреймворк
- **Exposed** - ORM для работы с БД
- **SQLite** - база данных
- **JWT** - аутентификация
- **CORS** - поддержка кросс-доменных запросов

### Frontend:
- **HTML/CSS/JavaScript** - статические страницы
- **Fetch API** - для работы с backend
- **LocalStorage** - для хранения JWT токенов

## 🐛 Troubleshooting

### Backend не запускается
```bash
# Очистка и пересборка
./gradlew clean
./gradlew :backend:build
./gradlew :backend:run
```

### Ошибка "Address already in use"
```bash
# Найти и завершить процесс на порту 8080
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# Или использовать другой порт в Application.kt
```

### CORS ошибки в браузере
- Убедитесь, что backend запущен на порту 8080
- Проверьте, что веб-сервер запущен на порту 3000
- CORS настроен для localhost:3000 и 127.0.0.1:3000

### Web-сервер не запускается
```bash
# Альтернативные способы запуска HTTP сервера:

# Python 3
python3 -m http.server 3000

# Node.js (если установлен)
npx http-server -p 3000

# PHP (если установлен)
php -S localhost:3000
```

## 🎯 Что можно делать

### После запуска вы можете:

1. **Войти в систему** через веб-интерфейс (user/user)
2. **Зарегистрировать новых пользователей**
3. **Тестировать API** через curl или Postman
4. **Разрабатывать новую функциональность**
5. **Добавлять новые страницы** в web-frontend
6. **Расширять API** в backend

## 📝 Полезные команды

```bash
# Сборка всего проекта
./gradlew build

# Запуск только backend
./gradlew :backend:run

# Запуск тестов
./gradlew test

# Очистка проекта
./gradlew clean

# Проверка статуса Gradle Daemon
./gradlew --status
```

## 🚀 Готово к разработке!

После выполнения этих шагов у вас будет полностью рабочая система:
- ✅ Backend API с базой данных
- ✅ Веб-интерфейс с аутентификацией  
- ✅ Тестовые данные для начала работы
- ✅ CORS настроен для локальной разработки

Можно начинать разработку новых функций! 🎉